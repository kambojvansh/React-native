import React from 'react';
import { Text, StyleSheet } from 'react-native';

export default class Insta extends React.component {
    render() {
        return (
            <Text>Insta page</Text>
        )
    }
}